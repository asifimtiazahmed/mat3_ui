
import 'package:flutter/material.dart';

/// Rectangle with Radius 4
final rectWithRadius4 = RoundedRectangleBorder(
  borderRadius: BorderRadius.circular(4),
);

/// Rectangle with Radius 6
final rectWithRadius6 = RoundedRectangleBorder(
  borderRadius: BorderRadius.circular(6),
);

/// Rectangle with Radius 8
final rectWithRadius8 = RoundedRectangleBorder(
  borderRadius: BorderRadius.circular(8),
);
